---
# tasks file for itspirit.bootstrap

- name: Update all packages to the latest version.
  apt:
    upgrade: safe
    update_cache: yes
    cache_valid_time: 3600


- name: Ensure acl filesystem support is available.
  apt:
    pkg: acl
    state: present
